/*function onresize() {
    var h = jQuery(window).height(), w = jQuery(window).width();

    if (w < 400) {
        jQuery('.nav').attr('class', 'nav nav-pills nav-stacked');
    } else {
        jQuery('.nav').attr('class', 'nav nav-tabs');
    }
}
jQuery(window).resize(onresize);

onresize();*/
